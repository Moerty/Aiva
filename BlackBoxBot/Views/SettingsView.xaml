<Controls:MetroContentControl x:Class="BlackBoxBot.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
			 xmlns:Controls="http://metro.mahapps.com/winfx/xaml/controls"
             mc:Ignorable="d" 
             d:DesignHeight="550" d:DesignWidth="1092">
    <Grid>
        <ComboBox x:Name="cbLanguage" HorizontalAlignment="Left" Margin="846,14,0,0" VerticalAlignment="Top" Width="231" SelectedIndex="0"/>
        <TextBlock x:Name="textBlock" HorizontalAlignment="Left" Margin="799,24,0,0" TextWrapping="Wrap" Text="Sprache" VerticalAlignment="Top"/>
        <TabControl ItemsSource="{Binding Model.SettingsTabs}" HorizontalAlignment="Left" Height="550" VerticalAlignment="Top" Width="650" TabStripPlacement="Left" Style="{StaticResource CustomTabControlStyle}">
            <TabControl.ItemContainerStyle>
                <Style TargetType="TabItem" BasedOn="{StaticResource CustomTabItemStyle}">
                    <Setter Property="Header" Value="{Binding Header}" />
                    <Setter Property="Content" Value="{Binding Content}" />
                </Style>
            </TabControl.ItemContainerStyle>
        </TabControl>



    </Grid>
</Controls:MetroContentControl>
